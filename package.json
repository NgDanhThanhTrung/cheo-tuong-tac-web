{
  "name": "cheo-tuong-tac-monorepo",
  "version": "9.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix client",
    "build": "npm run install-all && npm run build --prefix client",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "mongoose": "^9.9.1",
    "node-cron": "^4.6.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
